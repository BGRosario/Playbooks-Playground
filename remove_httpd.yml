---

- hosts: all
  become: true 
  tasks:

  - name: install httpd/apache packages with dependencies
    dnf: 
      name: httpd 
      state: absent

  - name: Disable HTTP
    firewalld:
      service: http
      permanent: true
      state: disable
      immediate: true
